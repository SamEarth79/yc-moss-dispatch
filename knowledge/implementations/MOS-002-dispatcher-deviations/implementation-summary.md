
## MOS-STORY-002-001: Deviation index + seed (QA note)

What was tested and why: `backend/test_deviation_index.py` validates the seed data contract (3-4 records, `type: deviation`, `seed: "true"`, all-string metadata, `protocolChunkId` values that exist in `protocol_chunks.py`) because Moss metadata must be strings and broken chunk references would silently degrade retrieval. Lifespan tests use a fake MossClient to prove the server still starts, and unloads only what loaded, when the deviation index reports failure or raises, while a required-index failure still raises. Further tests cover LIVE_LOADED_INDEXES membership, reload-on-mutation for `deviation-index`, and `/api/indexes` listing. No E2E: no user-facing surface. The build script and real Moss index creation are unverified against a live Moss project.

## MOS-STORY-002-002: Judge endpoint (QA note)

What was tested and why: `backend/test_deviation_judge.py` unit-tests `judge_deviation` against a fake AsyncOpenAI-style client (unconfigured returns None, valid followed/deviated output, and every malformed-output path raising ValueError) so the endpoint can rely on that contract. Feature tests drive `POST /api/deviations/judge` through TestClient with a fake `moss_client` and mocked `judge_deviation`, covering validation (422), 503/502 with no writes, followed writing nothing, the exact shape of the single deviated document, create-vs-add index choice, reload and `retrievable`, 500 on write failure, and no leakage of provider or exception detail. No E2E: no UI. Caveat: real DeepSeek verdict quality, real Moss create_index/add_docs behavior and load_index reload semantics are unverified (mocked only), so the story is PASS WITH CAVEATS.

## MOS-STORY-002-003 testing note

Added `backend/test_deviation_retrieval.py` (8 tests) driving `transcript_worker` through the real `/ws` endpoint with a fake `moss_client` (records index, query text, top_k) and mocked LLM extraction, so no real Moss/DeepSeek is called. Covers concurrent protocol top-1 / deviation top-2 queries on the same trailing window, score filtering incl. the exact cutoff, payload shape and ordering, the dev-log entry with latency, graceful degradation when the deviation query raises, and an integration where a deviation saved via `POST /api/deviations/judge` is returned by a subsequent transcript without restart. Caveat: `DEVIATION_MIN_SCORE=0.3` is untuned and Moss's score scale is unverified; everything is mock-only, hence PASS WITH CAVEATS.

## MOS-STORY-002-007 testing note

Added `backend/test_deviations_list.py` (9 tests) driving `GET /api/deviations` through TestClient with a fake `moss_client`: missing index and empty index give `[]`, newest-first ordering, exact record shape and metadata defaults, malformed callerSummary becoming `{}`, the seed flag, and 500 with a safe message and no leaked detail for both lookup and fetch failures. Added `backend/tests/e2e/test_deviations_page.py` (11 Playwright tests) against the real static files via the existing stub harness (extended with a stateful `/api/deviations` route): populated newest-first list, singular/plural count, sample tag, empty state, error + Retry recovery, clamp/show-more toggle, read-only page, landmarks/title, nav links both ways, and a narrow-viewport overflow edge case. No arbitrary sleeps; Playwright auto-waiting only. Caveat: E2E uses a stub server and Moss's missing-index error type is mock-assumed, hence PASS WITH CAVEATS.

## MOS-STORY-002-004 testing note

Added `backend/tests/e2e/test_dispatcher_panel.py` (17 Playwright tests) with its own stub app serving the real `index.html`/`app.js`/`style.css`: a `/ws` stub that accepts the socket and pushes `protocol_update` on demand, and a configurable `POST /api/deviations/judge` stub that records request bodies and can hold its response. Tests cover panel labels, mock-button behaviour, every Submit-disabled condition including in-flight double-submit, the click-time chunk snapshot, optional reason, followed/deviated/not-retrievable cards, 503/502/500 errors with preserved inputs, Ctrl+Enter, caller-change reset, card persistence, and role attributes. Waits are Playwright assertions only. Caveat: stubs stand in for the real LLM verdict and WebSocket, and accessibility is checked via attributes only, hence PASS WITH CAVEATS.

## MOS-STORY-002-005 testing note

Added `backend/test_dispatcher_voice.py` (11 feature tests): a fake `DeepgramStream` records start/stop/audio and emits a transcript through the server-supplied callback when audio arrives, so results land on the server loop deterministically. It covers default and dispatcher channels, status tagging, separate transcript routing (Moss never queried for dispatcher text), caller regression, unknown channel, unavailable per channel, audio routing, restart-stops-previous and disconnect cleanup. Added `backend/tests/e2e/test_dispatcher_mic.py` (11 Playwright tests) with its own stub `/ws` that records client messages and can push `voice_status`/`dispatcher_voice_transcript`; it uses Chromium fake-media flags with a granted microphone so the real getUserMedia/AudioWorklet path runs, except the denial test (getUserMedia override). It covers start/stop messages, listening UI, append-to-existing text, mutual exclusion both ways, blocked mocks/Submit, error routing and caller regression. Result: 22 new tests pass; the full suite has 1 failure in an existing 002-004 test because the `.voice-controls` group `aria-label` now contains "Dispatcher reply" (ambiguous locator), reported to the orchestrator. Caveat: real Deepgram ASR and real microphone/speaker-bleed behaviour are unverified, hence PASS WITH CAVEATS after that fix.
