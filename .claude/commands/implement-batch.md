# /implement-batch (deprecated)

This command has been renamed to `/implement-epic`.

If you see this file invoked, redirect to `commands/implement-epic.md` and
use `/implement-epic <feature-id>` instead. The behavior is identical except
that `/implement-epic` accepts a short feature ID (e.g. `LIB-002`) and pulls
story order from Asana.
